{
  "name": "easycase",
  "private": true,
  "scripts": {
    "start": "meteor --settings private/settings.json",
    "ios": "meteor --settings private/settings.json run ios-device",
    "android": "meteor --settings private/settings.json run android-device"
  },
  "dependencies": {
    "azure-storage": "^2.6.0",
    "babel-runtime": "^6.20.0",
    "faker": "^4.1.0",
    "meteor-node-stubs": "~0.2.4",
    "stream-buffers": "^3.0.1"
  }
}
